<h1>User details</h1>

</ul>
<table>
  <tr>
    <th>User ID</th>
    <th>First Name</th>
    <th>Designation</th>
  </tr>
  <% @users.each do |user| %>
  <tr>
    <td><a href="/users/<%= user.id %>">
        <%= user.id %>
    </a></td>
    <td><%= user.first_name %></td>
    <td><%= user.role_id %></td>
  </tr>
  <% end %>
</table>
<%= link_to "New user", new_user_path %>