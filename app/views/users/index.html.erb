<h1>User details</h1>

</ul>
<table class="table table-hover">
  <tr>
    <th>User ID</th>
    <th>First Name</th>
    <th>Designation</th>
  </tr>
  <% @users.each do |user| %>
  <tr>
    <td><a href="/users/<%= user.id %>">
        <%= user.id %>
    </a></td>
    <td><%= user.first_name %></td>
    <td><%= user.role.designation %></td>
  </tr>
  <% end %>
    <% flash.each do |type, msg| %>
    <div class="alert alert-info">
     <%= msg %>
    </div>
  <% end %>
</table>
<%= link_to "New user", new_user_path %></br>
<%= link_to "Existing user", sign_in_path %></br>
<%= link_to "Search Canidates", candidates_path %>